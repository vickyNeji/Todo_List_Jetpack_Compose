<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todolistcompose">

    <application
        android:name=".NotesAndroidApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TodoListCompose">
        <activity
            android:name=".AddNoteActivity"
            android:exported="false"
            android:label="@string/title_activity_add_note"
            android:theme="@style/Theme.TodoListCompose.NoActionBar" />
        <activity
            android:name=".EditNoteActivity"
            android:exported="false"
            android:label="@string/title_activity_note_detail"
            android:theme="@style/Theme.TodoListCompose.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TodoListCompose.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>